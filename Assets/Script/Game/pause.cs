using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pause : MonoBehaviour {
	// Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {
		/*if (Input.GetMouseButtonDown (0)) {//タップされたとき　ボタンのx-40<マウスのx<ボタンのx+40 かつ　ボタンのy-40<マウスのy<ボタンのy+40
			Vector2 mouse = Input.MousePosition;
			if(2 < mouse.x && mouse.x < 2.8 && 4<mouse.y&& mouse.y<4.8){
			Time.timeScale = 0 ;
		}
		}*/
	}}
